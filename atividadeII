string: bemVindo = "Olá, Sua participacão é muito Importante para nós da Sanivis!";
console.log (bemVindo)
// Verifica data atual da rede com a data do evento
{const dataEvento = ("2021/07/13")
let hoje = LocalDate.now();}
{if (dataEvento < hoje) 
     var idade = idade
    console.log ("Informe sua Idade:") 
    let idade 
    if (idade < 18) 
    console.log ("Infelizmente esse evento é destinado à maiores de idade!")
 else (dataEvento < hoje) 
    console.log ("Infelizmente não foi possível realizar sua inscrição, o prazo ou data do evento foi expirado!")}

    {const dataEvento = ("2021/07/13")
    let hoje = LocalDate.now();}
    if (dataEvento < hoje)
    var listName = listName
    console.log ("Informe seu nome completo") 
    {let listName
    var contador
    contador = contador + 1}
 else (contador >= 100)
    console.log ("Infelizmente não foi possível realizar sua inscrição, o prazo ou data do evento foi expirado!")}
  let idade + listName = sucesso
var sucesso = sucesso
console.log ("Sua Inscrição foi realizada com Sucesso!")
